var Backbone = require('backbone');
var withIdentityMap = require('backbone-with-identity-map');


var Network = Backbone.Model.extend({
});

withIdentityMap.call(Network);


module.exports = Network;
